# 🚀 Быстрый старт - Тестирование исправления Claude моделей

## ✅ Что было сделано

Обновлен User-Agent с `1.11.9` на `1.13.3` для соответствия текущей версии Google Antigravity.

## 📋 Как протестировать

### Вариант 1: Запуск приложения (рекомендуется)

```bash
# 1. Запустить приложение
npm run tauri dev

# 2. В приложении:
#    - Откройте "API Proxy"
#    - Запустите прокси сервер
#    - Проверьте что выбран аккаунт

# 3. Сделайте тестовый запрос к Claude
```

### Вариант 2: Прямой API запрос

```bash
# Убедитесь что прокси запущен на порту 8045
curl -X POST http://127.0.0.1:8045/v1/messages \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer sk-antigravity" \
  -d '{
    "model": "claude-sonnet-4-5",
    "messages": [{
      "role": "user",
      "content": "Say hello"
    }],
    "max_tokens": 50
  }'
```

## ✅ Ожидаемый результат

**До исправления:**
```json
{
  "error": {
    "message": "Quota exceeded...",
    "type": "rate_limit_error"
  }
}
```
Status: **429**

**После исправления:**
```json
{
  "id": "msg_...",
  "type": "message",
  "content": [{
    "type": "text",
    "text": "Hello!"
  }],
  "model": "claude-sonnet-4-5"
}
```
Status: **200** ✅

## 🔍 Проверка логов

```bash
# Найти файл логов
ls -lt ~/Library/Application\ Support/com.lbjlaq.antigravity-tools/logs/ | head -2

# Проверить User-Agent
grep "UpstreamClient User-Agent" ~/Library/Application\ Support/com.lbjlaq.antigravity-tools/logs/antigravity_*.log | tail -1
```

**Должно быть:**
```
🔧 UpstreamClient User-Agent: antigravity/1.13.3 darwin/arm64
```

## 🐛 Если все еще 429

### 1. Проверьте версию в логах
Убедитесь что используется `1.13.3`, а не `1.11.9`

### 2. Попробуйте другую платформу
```bash
# Для Intel Mac
export CLAUDE_USER_AGENT="antigravity/1.13.3 darwin/amd64"

# Для Windows
export CLAUDE_USER_AGENT="antigravity/1.13.3 windows/amd64"

# Перезапустите приложение
npm run tauri dev
```

### 3. Включите debug режим
```bash
export DEBUG_CLAUDE_REQUESTS=1
npm run tauri dev

# Проверьте детальные логи запросов
```

### 4. Проверьте актуальную версию Antigravity
В оригинальном Google Antigravity:
- Help → About
- Проверьте версию
- Если версия новее 1.13.3, обновите в коде

## 📁 Измененные файлы

- `src-tauri/src/proxy/upstream/client.rs` - Обновлен User-Agent
- `src-tauri/src/proxy/upstream/enhanced_logging.rs` - Расширенное логирование (новый)
- `src-tauri/src/proxy/upstream/debug_variants.rs` - Варианты для тестирования (новый)
- `scripts/test_claude_variants.sh` - Автотест скрипт (новый)

## 📞 Результаты

После тестирования сообщите:
- ✅ Работает / ❌ Не работает
- HTTP статус код
- Сообщение об ошибке (если есть)
- User-Agent из логов

---

**Следующий шаг:** Запустите приложение и протестируйте Claude Sonnet/Opus! 🚀
