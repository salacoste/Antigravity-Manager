# 🔍 Инструкция по перехвату запросов

## Цель
Перехватить и сравнить запросы от:
1. ✅ Оригинального Google Antigravity (работает)
2. ❌ Antigravity Manager (не работает с Claude)

## 📋 Шаг 1: Запустить перехватчик

Откройте терминал и запустите:

```bash
cd /Users/r2d2/Documents/Code_Projects/00_mcp/Antigravity-Manager/.debug
./start_capture.sh
```

**Оставьте этот терминал открытым!** Он будет перехватывать запросы.

## 📋 Шаг 2: Перехватить запрос от ОРИГИНАЛЬНОГО Antigravity

### A. Настроить прокси в системе (временно!)

**macOS:**
```bash
# В отдельном терминале:
networksetup -setwebproxy "Wi-Fi" 127.0.0.1 8888
networksetup -setsecurewebproxy "Wi-Fi" 127.0.0.1 8888
```

### B. Открыть оригинальный Google Antigravity

1. Запустите приложение Antigravity
2. Откройте любой файл
3. Используйте Claude Sonnet или Opus (например, спросите "What is 2+2?")
4. Подождите ответа

### C. Отключить прокси

```bash
networksetup -setwebproxystate "Wi-Fi" off
networksetup -setsecurewebproxystate "Wi-Fi" off
```

**Результат:** В терминале с mitmproxy вы увидите захваченные запросы

## 📋 Шаг 3: Перехватить запрос от Antigravity Manager

### A. Настроить прокси для Rust приложения

```bash
export HTTP_PROXY=http://127.0.0.1:8888
export HTTPS_PROXY=http://127.0.0.1:8888
export REQUESTS_CA_BUNDLE=""
export SSL_CERT_FILE=""
```

### B. Запустить Antigravity Manager с проксированием

```bash
cd /Users/r2d2/Documents/Code_Projects/00_mcp/Antigravity-Manager
npm run tauri dev
```

### C. В приложении:

1. Откройте "API Proxy"
2. Убедитесь что прокси запущен на порту 8045
3. Выберите аккаунт

### D. Сделать тестовый запрос

```bash
curl -X POST http://127.0.0.1:8045/v1/messages \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer sk-antigravity" \
  -d '{
    "model": "claude-sonnet-4-5",
    "messages": [{"role": "user", "content": "What is 2+2?"}],
    "max_tokens": 50
  }'
```

**Результат:** В терминале с mitmproxy вы увидите захваченные запросы

## 📋 Шаг 4: Остановить перехватчик

В терминале с mitmproxy нажмите `Ctrl+C`

## 📊 Шаг 5: Анализ результатов

Все захваченные запросы сохранены в:
```
/Users/r2d2/Documents/Code_Projects/00_mcp/Antigravity-Manager/.debug/captures/
```

Файлы:
- `request_TIMESTAMP_001.json` - запрос от оригинального Antigravity
- `response_TIMESTAMP_001.json` - ответ для оригинального
- `request_TIMESTAMP_002.json` - запрос от Antigravity Manager
- `response_TIMESTAMP_002.json` - ответ для Manager

## 🔍 Что смотреть при сравнении:

### В файлах request_*.json:

1. **User-Agent** (в headers):
   - Оригинал: `antigravity/1.13.3 ...`
   - Наш: должен быть таким же!

2. **requestType** (в body):
   - Оригинал: `"agent"` или другое?
   - Наш: что у нас?

3. **model** (в body):
   - Оригинал: `"claude-sonnet-4-5"` или другой формат?
   - Наш: как мы отправляем?

4. **Другие headers**:
   - Есть ли дополнительные headers у оригинала?
   - X-Goog-* headers?

### В файлах response_*.json:

1. **status_code**:
   - Оригинал: 200 ✅
   - Наш: 429 ❌

2. **error message** (если есть):
   - Какая именно квота?
   - Есть ли подсказки?

## ⚠️ Важные замечания

1. **SSL Certificate**: mitmproxy создает свой сертификат. Если приложение жалуется на сертификат, это нормально для теста.

2. **Системный прокси**: Не забудьте отключить системный прокси после теста!
   ```bash
   networksetup -setwebproxystate "Wi-Fi" off
   networksetup -setsecurewebproxystate "Wi-Fi" off
   ```

3. **Environment variables**: Очистите после теста:
   ```bash
   unset HTTP_PROXY
   unset HTTPS_PROXY
   ```

## 🎯 После захвата

Я проанализирую файлы и найду точное отличие!
